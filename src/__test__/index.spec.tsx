import { shallow } from 'enzyme';

describe('App', () => {
  describe('pages', () => {
    describe('Index', () => {
      it('renders correctly', () => {
        shallow(<div></div>);
      });
    });
  });
});
